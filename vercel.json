{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/**/*.py": {
      "includeFiles": "backend/model.joblib"
    }
  },
  "rewrites": [
    {
      "source": "/predict(.*)",
      "destination": "/api/index.py"
    }
  ]
}
