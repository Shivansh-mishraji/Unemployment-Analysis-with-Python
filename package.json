{
  "name": "unemployment-analysis",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && node -e \"const fs=require('fs'); fs.rmSync('public',{recursive:true,force:true}); fs.cpSync('frontend/dist','public',{recursive:true});\""
  }
}
